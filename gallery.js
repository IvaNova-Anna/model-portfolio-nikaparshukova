/**
 * Порядок и разбивка галереи на ряды.
 *
 * Каждый вложенный массив — один ряд на странице, сверху вниз. Внутри ряда
 * фото идут слева направо. Имена — без папки и без расширения, ровно как
 * файл называется в photos/.
 *
 * Ряд из двух кадров читается как разворот, из трёх — как серия. Ширина
 * внутри ряда делится по пропорциям кадров, так что ничего не обрезается.
 * На телефоне любой ряд разворачивается в одну колонку, порядок сохраняется.
 *
 * Пример:
 *
 *   export const rows = [
 *     ['01-portrait', '02-studio'],
 *     ['03-street', '04-street', '05-street'],
 *     ['06-editorial', '07-editorial'],
 *   ];
 *
 * Фото, которое лежит в photos/, но не попало ни в один ряд, не потеряется:
 * оно уедет в конец галереи. Но лучше так не оставлять — тест
 * gallery-rows.test.js напомнит про каждое такое фото.
 */
export const rows = [];
